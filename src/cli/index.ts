#!/usr/bin/env node

/*
 Handle command-line input commands here, for when the module will be installed globally using `npm install -g` or
 'yarn global add`
 */

function handleCliInput() {}

handleCliInput();
